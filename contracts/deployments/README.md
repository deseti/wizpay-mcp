# Deployment staging boundary

No deployment is enabled in Phase 0. The reviewed inventory is in `docs/blockchain-inventory.md`. Future machine-readable deployment records must include network, numeric chain ID, address, source, verification evidence, contract/ABI version, purpose, allowed domain, and explicit enablement status. Guessed addresses are forbidden.

